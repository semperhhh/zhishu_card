import 'package:flutter/material.dart';

class SettingSendVC extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("推送设置"),
      ),
      body: Container(
        child: Text("推送设置"),
      ),
    );
  }
}
